<% @rooms.each do |room| %>
	<%= form_for(current_user.memberships.build, remote: true) do |f| %>
	  <div><%= hidden_field_tag :room_id, room.id %></div>
	  <%= f.submit "Follow", class: "btn btn-primary" %>
	<% end %>
